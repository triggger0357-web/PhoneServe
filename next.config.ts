   const nextConfig = {
     devIndicators: {
       appIsrStatus: false,
     },
     experimental: {
       allowedDevOrigins: ["localhost:3000", "127.0.0.1:8080"]
     }
   };

   export default nextConfig;
